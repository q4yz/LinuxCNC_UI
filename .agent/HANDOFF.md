# Agent Handoff — Recent Work and Open Work

> **Optional document.** This file is hand-curated by previous AI
> agents (or humans) to pass context to the next agent. It is not
> generated by the build and is safe to delete. The ``test_doc_links``
> suite treats it as optional — if the file is missing, the
> parametrized cases simply skip without failing.

This file is the agent's "lab notebook". Historical session arcs and
resolved items were moved out — everything still open is tracked as a
GitHub issue (see § 2); the issues are the canonical backlog now.

---

## 1. Recent attempted work (newest first)

### 1.0 (2026-09) Machine-online gating, default-machine lifecycle, install.sh cleanup

- **Machine-online gating (replaces the freeze watchdog).** The old
  pending-snapshot watchdog / "Connection appears stuck" modal was
  removed entirely (it false-positived on the split backend's
  snapshot service answering 502). Replacement: a global heartbeat in
  [frontend/src/composables/useMachineOnline.ts](frontend/src/composables/useMachineOnline.ts)
  probing `GET /api/v1/health` (new route in `backend/machine/main.py`)
  every 3 s with a 2 s `AbortSignal.timeout`; `App.vue` starts/stops
  the 1 Hz snapshot poll and the telemetry WebSocket on its verdict
  (750 ms settle debounce on connect; a `manualClose` flag in
  [frontend/src/facades/servoThreadFacade.ts](frontend/src/facades/servoThreadFacade.ts)
  prevents reconnect loops). `MachineGate.vue` renders
  skeleton / offline-card-with-Start / slot; machine-level widgets in
  Dashboard, Running, Jogging, Debug, Files and Settings views are
  gated, which also kills a hung camera MJPEG stream on unmount.

- **Default-machine lifecycle (system service).**
  [backend/system/services/MachineLifecycleService.py](backend/system/services/MachineLifecycleService.py)
  persists `machine_config/default_machine.json`; `POST
  /api/v1/system/machine/start` accepts an optional `{machine}` body
  (persist-as-default + start) and `POST /default` selects without
  starting. The INI resolver accepts BOTH layouts:
  `machines/<name>/config/machine.ini` and
  `machines/<name>/configs/machine.ini` (the generator output all real
  machines use — the first cut only looked for `config/` and 404'd on
  every real machine). UI: `MachinesExplorer.vue` Start (▶) / Set-main
  (★) buttons on root-level machine folders, `UpdateManager.vue`
  Start-default/Stop controls with a live status line.

- **install.sh cleanup.** The phantom `linuxcnc-ui-machine` unit is
  gone (enable/restart/sudoers references removed — the machine
  backend is a program started/stopped by the system service, NOT a
  unit; the spawn feature itself is still missing, see issue #121).
  Added a `linuxcnc-ui-cert.service` oneshot that runs
  `start_network.sh` at boot to re-mint the TLS certificate for the
  current IPs + reload nginx. `scripts/update.sh` and
  `rebuild_ui.sh` unit-name fixes landed in the same pass.

- **Backend WS heartbeat.** `ServoThreadService.telemetry_loop`
  broadcasts a ~1 Hz `{"type": "heartbeat"}` envelope even when idle
  (an idle machine previously sent no frames at all); the facade
  stamps `lastMessageAt` on every frame. Kept as diagnostics — the
  freeze watchdog that consumed it was removed.

- **Known breakage from the same window.** The mypy-packaging pass
  added `backend/__init__.py`, which broke machine-suite pytest
  collection (bare-name `tests` package shadowing) and re-exposed ~33
  pre-existing failures — the conftest fix landed but the session was
  interrupted before re-verifying. See issue #118 before trusting
  `pytest backend/machine/tests` results.

---

## 2. Open work — tracked on GitHub

The backlog lives in the repo's issues now; this section is only a
map. **Do not close an issue from here without reading it there.**

| Issue | Topic |
|---|---|
| #111 | ESLint + `@typescript-eslint/no-explicit-any` (TS typing gate; mypy half already done) |
| #116 | `newline=""` on remaining `write_text()` sites (CRLF corrupts machine.hal → POSIX controller) |
| #117 | ~56 pre-existing frontend test failures (identical on clean checkout) |
| #118 | machine tests: packaging broke collection (conftest fix unverified) + ~33 failures |
| #119 | Clear the mypy `disable_error_code` backlog (~90 design-level errors) |
| #120 | PascalCase sweep for core infra (`backend/common/core/settings_store.py`, `event_bus.py`) |
| #121 | System service must spawn/stop the machine backend (:8000) — nothing starts it on a fresh install |
| #112 | Machine.hal wiring generation (needs hardware.json pin-out schema) |
| #113 | Fragile `Stepper.section_name` computed property |
| #114 | Deploy scripts / nginx two-backend routing never exercised on real hardware |
| #115 | Backend layering exceptions (documented won't-fix list) |

**Policy note (not an issue).** `backend/requirements-machine.txt` and
`backend/requirements-system.txt` are placeholders (`-r
requirements.txt`) on purpose — they exist so a future machine-only or
system-only dependency has an obvious home. Don't let a new dependency
default into the shared `requirements.txt` out of laziness.

---

## 3. Anti-patterns observed in past attempts

- **Hard-coded G-code in `.vue` components.** Fixed in
  `frontend/src/config/gcodes.js`. Watch for new commits reintroducing
  `M3` / `G10` / `G91` literals in components.

- **Skipping `storeToRefs()`.** `const { droX } = useMachineStore()`
  captures the value at destructuring time and silently loses
  reactivity. Always use `storeToRefs()`.

- **Cross-store imports at module scope.** A `useConsoleStore()`
  import at the top of a Pinia store file broke the other store's
  initialization. Cross-store dependencies belong inside the action
  method, not at module scope.

- **Hardcoded `localhost` / `127.0.0.1` in JS.** The frontend resolves
  the API host via `window.location.hostname`; the Vite dev server
  binds `0.0.0.0` so the same build works on a phone, tablet, or shop
  workstation.

- **Bypassing the jog watchdog.** `jog_watchdog.py` reads its timeout
  **once** at startup. Moving the read into a hot path would let the
  watchdog window grow beyond the documented 500 ms — a safety
  regression.

---

## 4. Pointers for the next agent

- Read [`.agent/context/hub.md`](.agent/context/hub.md) first.
- `.agent/TEST.md` is the canonical verification gate (mypy + pytest ×
  3 suites — separate invocations, see ARCHITECTURE.md § 1 — plus
  frontend typecheck/test/build).
- Rough surface today: backend ~600 tests across the three suites
  (machine suite currently broken — see #118), frontend ~330 tests
  (56 known failures — see #117).
- The backend is split into `backend/machine` (:8000, may be down),
  `backend/system` (:8001, always up) and `backend/common` (shared
  library) — see [`.agent/context/ARCHITECTURE.md`](.agent/context/ARCHITECTURE.md).

---

## 5. Maintainer notes

This file is agent-maintained. When you finish a session, prepend a
short "Recent attempted work" entry with:

- What you did.
- The status (complete / partial / abandoned).
- Any follow-up the next agent should know — **as a GitHub issue, not
  as prose here** (open work belongs in the tracker; this file maps it).

Keep the file under ~200 lines. If a section grows past that, migrate
the content into [`.agent/context/LESSONS_LEARNED.md`](.agent/context/LESSONS_LEARNED.md)
and reset the section to a single-line pointer.
