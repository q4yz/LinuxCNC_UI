"""Jog dispatch service.

Previously named ``hal``; renamed to ``hal_service`` so it no longer
shadows the top-level ``hal`` Python binding that
:mod:`hardware.Connection` imports to talk to LinuxCNC.
"""